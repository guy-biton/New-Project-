<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Comunication_LTD</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 min-h-screen">
    <nav class="bg-blue-600 p-4 text-white">
        <div class="container mx-auto flex justify-between">
            <a href="/" class="font-bold text-xl">Comunication_LTD</a>
            <div>
                <% if (typeof user !== 'undefined' && user) { %>
                    <span class="mr-4">Welcome, <%= user.username %></span>
                    <a href="/auth/logout" class="bg-red-500 hover:bg-red-700 px-3 py-1 rounded">Logout</a>
                <% } else { %>
                    <a href="/auth/login" class="mr-4 hover:underline">Login</a>
                    <a href="/auth/register" class="hover:underline">Register</a>
                <% } %>
            </div>
        </div>
    </nav>
    <div class="container mx-auto p-4">
